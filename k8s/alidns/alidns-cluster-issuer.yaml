apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: alidns-cluster-issuer
spec:
  acme:
    email:  tangsong0202@gmail.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: alidns-account-key
    solvers:
    - dns01:
        webhook:
          groupName: acme.ohash.com
          solverName: alidns
          config:
            region: ""
            accessKeySecretRef:
              name: alidns-secret
              key: access-key
            secretKeySecretRef:
              name: alidns-secret
              key: secret-key
